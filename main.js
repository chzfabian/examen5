//* Parte 2: Corrección de Código en JavaScript 

/* function operar(a, b, callback) {
    return callback(a, b);
}

function resta(x, y) {
    x - y; 
}

let resultado = operar(15, "5", resta); 

console.log("El resultado de la operación es:", resultado);
 'El resultado de la operación es:' 10 */

(
 Ejercicio Realizado con una correcion

 function operar (a,b, callback) {
    return callback(a, b);
 }

 function resta(x,y) {
    return x -y;
 }
 let resultado = operar(15,("5"), resta);
 console.log("El resultado de la operacion es:", resultado);
 // el resultado de la operacion es: 10

 